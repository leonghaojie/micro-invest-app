# Copy to .env and adjust. Expo inlines EXPO_PUBLIC_* vars at build time.
# Use your machine's LAN IP (not localhost) when testing on a physical
# device via Expo Go, e.g. http://192.168.1.23:4000
EXPO_PUBLIC_API_BASE_URL=http://localhost:4000
